ApplicationModules.push (angular) ->
    angular.module("core").controller "CoreController", [
        "$scope"
        "$location"
        ($scope, $location) ->

            # initialize
            $scope.pageTitle = "BusinessCard7"
    ]
